# wp-blog-widget

## Dev Setup

``` bash
# install dependencies
npm install

# install basic node webserver
npm install -g http-server

# serve the widget index.html on http://localhost:8080
# NOTE: adjust port by adding port flag: '-p 9090'
http-server ./ 
